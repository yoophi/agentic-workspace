{
  "name": "acp-minimal-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@9.10.0",
  "scripts": {
    "dev": "turbo run dev --filter=@acp/desktop",
    "build": "turbo run build",
    "check-types": "turbo run check-types",
    "test": "turbo run test",
    "preview": "turbo run preview --filter=@acp/desktop",
    "tauri": "turbo run tauri --filter=@acp/desktop --",
    "tauri:dev": "turbo run tauri:dev --filter=@acp/desktop --"
  },
  "devDependencies": {
    "turbo": "^2.5.8"
  }
}
