import{describe,expect,it}from"vitest";import{createRedactedDiagnostics}from"./create-redacted-diagnostics";it("contains no path, document text, or annotation",()=>{const output=createRedactedDiagnostics({appVersion:"1",commit:"c",platform:"macOS",errorCodes:["E_IO"],rootCount:2,sessionCount:3});expect(output).not.toContain("/Users/");expect(Object.keys(JSON.parse(output))).not.toContain("annotations");});
