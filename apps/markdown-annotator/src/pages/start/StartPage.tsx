import{useEffect,useState}from"react";import{open}from"@tauri-apps/plugin-dialog";import{invoke}from"@tauri-apps/api/core";import{Button}from"@/components/ui/button";type Recent={path:string;kind:"folder"|"document"};
export function StartPage(){const[recent,setRecent]=useState<Recent[]>([]);useEffect(()=>{if("__TAURI_INTERNALS__" in window)void invoke<Recent[]>("load_recent_targets").then(setRecent)},[]);async function choose(directory:boolean){const path=await open({directory,multiple:false,filters:directory?undefined:[{name:"Markdown",extensions:["md","markdown"]}]});if(typeof path==="string")await invoke("request_open_document_window",{path});}return <main className="mx-auto flex min-h-screen max-w-3xl flex-col justify-center gap-8 p-10"><h1>Markdown Annotator</h1><p>Markdown 문서를 읽고, 원문을 바꾸지 않은 채 검토 의견을 남기세요.</p><div><Button onClick={()=>void choose(true)}>폴더 열기</Button><Button onClick={()=>void choose(false)}>Markdown 파일 열기</Button></div>{recent.length?<section><h2>최근 항목</h2><ul>{recent.map((item)=><li key={item.path}><button onClick={()=>void invoke("request_open_document_window",{path:item.path})}>{item.kind==="folder"?"폴더":"문서"} · {item.path}</button></li>)}</ul></section>:null}<ol><li>폴더 또는 문서를 엽니다.</li><li>문구를 선택해 annotation을 작성합니다.</li><li>피드백을 복사하거나 파일로 저장합니다.</li></ol><Button variant="ghost" onClick={()=>void invoke("open_settings_window")}>설정</Button><Button variant="ghost" onClick={()=>void invoke("open_about_window")}>정보</Button></main>}
