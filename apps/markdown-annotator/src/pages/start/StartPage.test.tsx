import{expect,it}from"vitest";import{renderToStaticMarkup}from"react-dom/server";import{StartPage}from"./StartPage";it("shows open actions and three-step guide without examples",()=>{const html=renderToStaticMarkup(<StartPage/>);expect(html).toContain("폴더 열기");expect(html).toContain("Markdown 파일 열기");expect((html.match(/<li>/g)??[])).toHaveLength(3);expect(html).not.toContain("예제");});
