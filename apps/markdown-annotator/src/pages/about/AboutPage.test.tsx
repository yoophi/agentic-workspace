import{expect,it,vi}from"vitest";vi.mock("@/entities/build-info/api/build-info-api",()=>({getBuildInfo:async()=>({version:"2026.8.2",commit:"abc",tag:"v",license:"MIT",notices:"notices"})}));import{renderToStaticMarkup}from"react-dom/server";import{AboutPage}from"./AboutPage";it("states formats, local-first, telemetry, license and notices",()=>{const html=renderToStaticMarkup(<AboutPage/>);expect(html).toContain(".md, .markdown");expect(html).toContain("Mac에만 저장");expect(html).toContain("telemetry");expect(html).toContain("라이선스");expect(html).toContain("오픈소스 고지");});
