import{useEffect,useState}from"react";import{getBuildInfo,type BuildInfo}from"@/entities/build-info/api/build-info-api";
export function AboutPage(){const[info,setInfo]=useState<BuildInfo|null>(null);useEffect(()=>{void getBuildInfo().then(setInfo)},[]);return <main className="mx-auto max-w-2xl space-y-4 p-8"><h1>Markdown Annotator</h1><p>지원 형식: .md, .markdown</p><p>모든 문서와 annotation은 이 Mac에만 저장되며 telemetry를 전송하지 않습니다.</p><dl><dt>버전</dt><dd>{info?.version??"불러오는 중"}</dd><dt>커밋</dt><dd>{info?.commit}</dd><dt>태그</dt><dd>{info?.tag}</dd><dt>라이선스</dt><dd>{info?.license}</dd><dt>오픈소스 고지</dt><dd>{info?.notices}</dd></dl><a href="https://github.com/yoophi/agentic-workspace">프로젝트 지원</a></main>}
