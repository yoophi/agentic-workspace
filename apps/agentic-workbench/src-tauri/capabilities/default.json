{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability set",
  "windows": ["main", "settings", "session-*"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "dialog:default"
  ]
}
