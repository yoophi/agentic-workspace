[package]
name = "git-core"
version = "0.1.0"
edition = "2021"
description = "Shared Git history/graph/commit-detail/file-diff core for git-explorer and agentic-workbench"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
