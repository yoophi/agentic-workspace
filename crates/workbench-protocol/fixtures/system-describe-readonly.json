{
  "name": "system-describe-readonly",
  "principal": "readonly",
  "request": { "protocolVersion": 1, "operation": "system.describe", "requestId": "r-describe-readonly", "input": {} },
  "expect": {
    "reply": {
      "kind": "complete",
      "output": {
        "protocolVersion": 1,
        "operations": [
          { "id": "project.list", "kind": "query", "requiredScopes": ["project:read"] },
          { "id": "system.describe", "kind": "query", "requiredScopes": ["system:describe"] }
        ]
      }
    },
    "schemaPresent": true
  }
}
